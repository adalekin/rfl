Import('env')

env = env.Clone()

""" Enable exceptions """
env['ENABLE_EXCEPTIONS'] = True

""" Build static library """
env.Append(LIBS = ['rightway_dump'])
env.ComponentTestProgram('test_dump', Glob('*.cpp'))

env.Depends(env.Alias('test_dump'), env.Alias('rightway_dump'))
