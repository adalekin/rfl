Import('env')

env = env.Clone()

""" Enable exceptions """
env['ENABLE_EXCEPTIONS'] = True

""" Build static library """
env.Append(LIBS = ['rightway_report', 'rightway_smtp'])
env.ComponentTestProgram('test_report', Glob('*.cpp'))

env.Depends(env.Alias('test_report'), env.Alias('rightway_report'))
env.Depends(env.Alias('test_report'), env.Alias('rightway_smtp'))
